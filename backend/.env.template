KITE_API_KEY=your_api_key_here
KITE_API_SECRET=your_api_secret_here
# After OAuth, the access token is saved in DuckDB (kite_session table in STORAGE_DIR/market_data.duckdb).
# Must match the redirect URL registered in your Kite Connect app (GET with ?request_token=...)
KITE_REDIRECT_URL=http://localhost:8000/api/data/auth/callback
CORS_ORIGINS=["http://localhost:3000"]
FRONTEND_URL=http://localhost:3000
